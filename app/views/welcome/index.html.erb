<div class="center jumbotron">
      <% if logged_in? %>
        <h1>Hello, <%= current_user.name %>!</h1>
      <% else %>
        <h1>Hello, User.</h1>
      <% end %>
  <%= link_to 'My Blog', controller: 'articles', class: "btn btn-primary" %>
</div>